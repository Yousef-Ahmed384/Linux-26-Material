# Description
- We have a folder called `scripts`, we want to run every new script added to this folder
ex: when user add `backup.sh` script, the system well detect it and run this `backup.sh` script 
- **use `inotifywait`**

# Submission
- but the commands or script used in the `solution.sh` file in `Task1` folder in your repo
- Follow this guidelines to submit: [Linux Tasks Repo Setup](https://gist.github.com/thisisamna/f677bf7fc531dc62bda8fa8ee00a0a9f) 














